---
import { brands } from '../data/brands';
import en from '../i18n/en.json';
import '../styles/brands.css';
const t = en as Record<string,string>;
---
<section id="brands" class="brands" data-brands aria-labelledby="brands-statement">
  <div class="brands-layout">
    <dl class="brands-stats">
      <div data-reveal><dt data-i18n="brands.years">{t['brands.years']}</dt><dd><span data-count-to="5">5</span></dd></div>
      <div data-reveal><dt data-i18n="brands.count">{t['brands.count']}</dt><dd><span data-count-to="15">15</span>+</dd></div>
    </dl>

    <div class="brands-grid">
      {[0,1].map(column=><div class="brands-column" data-brand-column={column}>
      {brands.filter((_,index)=>index%2===column).map(brand=>(
        <button
          type="button"
          data-brand-preview={`/imgs/brands/previews/${brand.file}.webp`}
          aria-controls="brand-preview" aria-expanded="false"
          class="brands-cell"
          style={`--brand-span:${brand.span};--brand-order:${brands.indexOf(brand)}`}
          data-brand-shape={['ISRA3','WEWHALE3','KA','WWF3','IMA3','EXO3','QW3','RH'].includes(brand.file) ? 'square' : 'wide'}
          data-brand-name={brand.name}
          aria-label={brand.name}
        >
          <img src={`/imgs/brands/${brand.file}.svg`} data-brand-original={`/imgs/brands/${brand.file}.svg`} data-brand-white={['WWF3','COA3','IMA3','SPERMWHALES3'].includes(brand.file)?`/imgs/brands/${brand.file}_W.svg`:undefined} alt="" loading="lazy" decoding="async" draggable="false" />
        </button>
      ))}
      </div>)}
    </div>

    <h2 id="brands-statement" class="brands-statement" data-reveal data-i18n="brands.statement">
      {t['brands.statement']}
    </h2>
  </div>

  <div id="brand-preview" class="brand-preview" hidden><figure><div class="brand-preview-image"></div><figcaption></figcaption></figure><button type="button" class="brand-preview-close" data-i18n="brands.close">{t['brands.close']}</button></div>
</section>
<script>
  import '../scripts/brands';
</script>
